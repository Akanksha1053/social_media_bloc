import 'package:flutter/material.dart';

const appBarIconColor = Color(0xFF130F26);
const appBarBackgroundColor = Color(0xFFFFFFFF);
const iconBackgroundColor = Color(0xFFE6EEFA);
const appBarTitleColor = Color(0xFF000000);

const postContainerColor = Color(0xFFE6EEFA);
const scaffoldBackgroundColor = Color(0xFFFFFFFF);
const postButtonColor = Color(0xFFDADADA);

const postBottomButtonDecoration = BoxDecoration(
  color: Color.fromRGBO(0, 0, 0, 0.3),
  borderRadius: BorderRadius.only(
    bottomLeft: Radius.circular(40),
    bottomRight: Radius.circular(40),
  ),
);

const postButtonIconColor = Color(0xFFFFFFCC);

const Color bottomNavBarColor = Color.fromRGBO(87, 144, 223, 0.6);
const Color bottomNavBarButtonBackgroundColor =
    Color.fromRGBO(255, 255, 255, 0.6);
const Color bottomNavBarBackColor = Color.fromRGBO(255, 255, 255, 0);
const Color bottomNavigationIconColor = Colors.white;
const Color activeBookmarkColor = Colors.red;
